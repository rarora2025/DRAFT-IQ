'use client'

/**
 * Minimal PushRegistration component to satisfy stale build graph references.
 * This file can be safely deleted once the dev server cache is cleared.
 */
export function PushRegistration() {
  return null
}
